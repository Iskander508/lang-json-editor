{
  "name": "lang_json_editor",
  "version": "0.1.0",
  "bin": "./cli.js",
  "dependencies": {
    "express": "^4.17.1",
    "open": "^8.0.2",
    "ws": "^7.4.4",
    "yargs": "^16.2.0"
  },
  "devDependencies": {
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "react-scripts": "4.0.1",
    "react-use-websocket": "^2.5.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build && rm -rf web && mv build web"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
